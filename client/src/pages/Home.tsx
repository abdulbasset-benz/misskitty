import HeroSection from "@/components/HeroSection";
import Collection from "@/components/Collection";

const Home = () => {
  return (
    <div>
      <HeroSection />
      <Collection/>
    </div>
  );
};

export default Home;
